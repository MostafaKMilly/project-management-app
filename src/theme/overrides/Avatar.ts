import { Components } from "@mui/material";

export const AvatarStylesOverrides: Components['MuiAvatar'] = {
    styleOverrides: {
        root: {
            border: "1px solid #fff"
        }
    }
}